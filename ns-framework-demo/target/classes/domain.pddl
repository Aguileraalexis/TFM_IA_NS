; This file is kept only as a reference artifact.
; The real travel domain is generated dynamically at runtime by:
; `com.tesis.nsdemo.travel.TravelDomainPddlGenerator`
;
; Use `GET /api/domain` to inspect the current generated domain.

(define (domain travel-dynamic-placeholder)
  (:requirements :strips)
  (:predicates
    (placeholder)
  )
)
